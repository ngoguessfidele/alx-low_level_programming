#include "main.h"

/**
 *  * _abs- absolute value
 *   * @num: an integer
 *    *
 *     * Return: integer
 *      */

int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	else if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}

